<p style="color: green"><%= notice %></p>

<h1>Clientes</h1>

<div id="clientes">
  <% @clientes.each do |cliente| %>
    <%= render cliente %>
    <p>
      <%= link_to "Mostrar este cliente", cliente %>
    </p>
  <% end %>
</div>

<%= link_to "Novo cliente", new_cliente_path %>
